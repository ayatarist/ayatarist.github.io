/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn-icons-png.flaticon.com',
      'upload.wikimedia.org',
      'logos-world.net'
    ],
  },
}

module.exports = nextConfig